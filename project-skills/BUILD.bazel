"""Canonical project-skills bundle embedded by graphforge-cli build.rs (#8)."""

package(default_visibility = ["//visibility:public"])

exports_files(glob(["**/*"]))

filegroup(
    name = "bundle",
    srcs = glob(
        ["**/*"],
        exclude = ["BUILD.bazel"],
    ),
)
