[project]
name = "graphforge-external-benchmarks"
version = "0.0.0"
description = "Dependency-isolated external benchmark orchestration for GraphForge"
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.0,<5",
  "reframe-hpc>=4.0,<5",
]

[tool.uv]
package = false
