Independent read-only review (separate agent, no builds), 2026-09-23, against datafusion 54.1.0 / arrow 58.4.0 sources and branch head 6bf02962.
1 CONFIRMED: Partitioning = RoundRobinBatch|Hash|UnknownPartitioning (datafusion-physical-expr-54.1.0/src/partitioning.rs:114-136); only range mention is `// TODO support RangePartition and OrderedDistribution` (datafusion-physical-plan-54.1.0/src/sorts/sort.rs:1144).
2 CONFIRMED: hash partitions fail monotone concatenation (live test asserts it); REPARTITION_RANDOM_STATE = SeededRandomState::with_seed(0) (repartition/mod.rs:576) over foldhash FixedState, whose crate docs (foldhash-0.1.5/src/lib.rs:11-13) disclaim cross-version/platform output stability; no recorded-split-point equivalent.
3 CONFIRMED: RoundRobinBatch is arrival-order, keyless.
4 CONFIRMED: arrow-ord partition.rs only finds equal-value runs in sorted input; no searchsorted/bucketize/split-point routing kernel in arrow-*-58.4.0.
5 CONFIRMED: single batch SortExec -> sort_batch_stream (sort.rs:607) -> sort_batch_chunked (677) -> lexsort_to_indices -> sort_to_indices for one column (arrow-ord sort.rs:939-942); FixedSizeBinary and LargeBinary both qualify (253-255); in-place threshold branch only for >1 in-memory batch.
6 CONFIRMED: partition.rs:11-19, graphforge-core/src/uuid.rs:3-9, partition/tests.rs:108 balance_assertion_refuses_formula_splitters_over_a_uuid_v7_ingest asserts full collapse and "skewed" refusal.
7 No correctness bugs: detail contiguity validated at runtime; into_vec recovers buffer; PeakPool observes after grow/try_grow; external-sort test asserts spill_count>0, spilled_rows>0, byte equality. Timing notes: fixed-mode Arrow/DF include adapter copies (disclosed; kernel-only workload isolates); range partition timing includes per-round sampling/plan (conservative against range).
