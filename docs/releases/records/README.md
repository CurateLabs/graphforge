# Release artifact records

JSON checksum / contents inventories produced by
`scripts/record_release_artifacts.py` for a frozen release-candidate commit.

See [`docs/development/release-artifact-record.md`](../../development/release-artifact-record.md).

Files named `vX.Y.Z-artifacts.json` are filled at freeze time; do not invent
checksums for unpublished builds.
