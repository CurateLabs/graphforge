[package]
name = "graphforge-cypher"
description = "GraphForge Cypher lexer and recursive-descent / Pratt parser"
version.workspace = true
edition.workspace = true
license.workspace = true
license-file.workspace = true
repository.workspace = true

[dependencies]
graphforge-core = { version = "0.5.2", path = "../graphforge-core" }
graphforge-ast = { version = "0.5.2", path = "../graphforge-ast" }

[dev-dependencies]
graphforge-ir = { version = "0.5.2", path = "../graphforge-ir" }
serde_json = { workspace = true }
divan = { workspace = true }

# Cypher front-end (lex → parse → bind) benchmarks (CodSpeed, simulation mode).
[[bench]]
name = "compile"
harness = false

[lints]
workspace = true
