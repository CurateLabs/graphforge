# napi build artifacts — generated by `napi build`, not committed.
# CI (and the publish job) build these fresh; consumers get them from the
# published npm package.
*.node
index.js
index.d.ts
npm/

# c8 / make coverage-node
coverage/
